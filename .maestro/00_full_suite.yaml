appId: fr.univ.nantes.archi
name: "AppliCash Full E2E Suite"
tags:
  - full-suite

# ── Global configuration ─────────────────────────────────────────────────────
# CA16: Each scenario must complete within 5 minutes.
# CA17: Loading states and network errors are handled via waitForAnimationToEnd
#        and optional assertions.

---

# Run all 4 scenarios in sequence on a clean install
- runFlow: 01_create_group.yaml
- runFlow: 02_add_expense_check_balances.yaml
- runFlow: 03_view_reimbursements.yaml
- runFlow: 04_edit_delete_expense.yaml

